# Mesh v0.1.x 发布确认单（CEO 验收版）

> 用途：发布前一页式确认。打勾即通过，未勾项即阻断发布。

---

## A. 功能完成度（必须）

- [x] 核心命令可用：`collect` / `query` / `list`
- [x] Markdown 兼容可用：`import` / `export`
- [x] 同步能力可用：`init` / `push` / `pull` / `sync`
- [x] Agent 协作可用：`agent register/list` / `inject`
- [x] Agent 协议文档完成：`docs/03-Agent协议.md`

---

## B. 质量门禁（必须）

- [x] 单元测试通过：`go test ./...`
- [x] 编译通过：`go build ./cmd/mesh`
- [x] 端到端回归通过（隔离环境）
- [x] help 文案与实际命令一致（含 `query`）

---

## C. 关键场景（必须）

- [x] import 重复数据可跳过（验证结果：新增 1，跳过 1）
- [x] sync 在冲突场景会自动备份本地库
- [x] query 支持关键词过滤
- [x] inject 支持 `--limit` / `--format`

---

## D. 文档与看板一致性（必须）

- [x] `TODO.md` 任务状态与代码一致
- [x] `tasks.md` 状态与 TODO 一致
- [x] 协议文档、命令文档链接可用

---

## E. 发布决策

- [ ] **同意发布 v0.1.x 候选版**
- [ ] **推迟发布（需补项）**

补项说明：

```
（若推迟发布，在这里写阻断项）
```

---

## F. 建议发布时间窗

- 建议：今天内完成 tag + 产物归档，明天开始小范围试用

---

**确认人**：晓力（CEO）  
**确认日期**：____-__-__  
**版本**：v0.1.x
